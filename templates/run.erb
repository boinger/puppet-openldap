#!/bin/sh
exec 2>&1
echo "*** Starting slapd..."
exec envuidgid <%= @user %> \
  /usr/sbin/slapd \
  -h  ldap:/// ldaps:/// ldapi:/// \
  -u <%= @runuser %> \
  -d 256 \
  -f /etc/openldap/slapd.conf
